spring:
  application:
    name: javaFinal

  datasource:
    url: jdbc:postgresql://localhost:5432/university_management_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080